# mysql/mysql-service.yaml
apiVersion: v1
kind: Service
metadata:
  name: mysql
  namespace: prod
spec:
  clusterIP: None  # Headless — required for StatefulSet DNS
  selector:
    app: mysql
  ports:
    - port: 3306
